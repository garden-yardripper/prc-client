[project]
name = "prc-client"
version = "0.1.0"
description = "A flexible, feature-rich API client for the ER:LC private server API."
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.ruff]
line-length = 120
select = ["E", "F"]